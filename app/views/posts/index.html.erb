<h1>トップページ</h1>
<% if @posts%>
  <% @posts.each do |post| %>
    <div class="post">
      <div class="post-name">
        <%= post.title %>
      </div>
      <div class="post-date">
        投稿日時：<%= post.created_at %>
      </div>
    </div>
  <% end %>
<% end %>